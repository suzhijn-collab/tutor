- 所有持久化数据（用户设置、聊天记录、错题本、好友列表）均通过 `localStorage` 进行本地存储，并在 `src/services` 目录下提供统一的 CRUD 接口。
- 页面组件统一放置在 `src/pages` 目录下，并以 PascalCase 命名（如 `Chat.tsx`, `WrongQuestions.tsx`），每个页面对应一个独立的功能模块。
- UI 交互广泛使用 `motion/react` 库实现声明式动画，包括页面切换的淡入淡出效果以及登录表单的展开/收缩过渡。