- **构建工具**：Vite 6.x 配合 `@vitejs/plugin-react` 进行快速开发与热更新。
- **样式方案**：Tailwind CSS v4 用于原子化样式编写，结合自定义渐变类（如 `soul-gradient`）实现视觉风格。
- **AI 模型**：集成豆包（Doubao）Seed 2.0 Mini 模型，通过 RESTful API 进行交互。
- **数学渲染**：使用 `react-markdown` 配合 `remark-math` 和 `rehype-katex` 实现 LaTeX 公式的高质量渲染。