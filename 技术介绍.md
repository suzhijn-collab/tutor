# 智课通 AI Tutor — 技术介绍

## 一、项目概述

**智课通 AI Tutor** 是一个面向高校学生的智能 AI 辅助学习平台，提供多领域 AI 导师对话、错题本、讨论区和帮助中心等功能。平台内置四个专业 AI Agent（高数酱、Py酱、Torch君、矩阵妹），分别覆盖微积分与线性代数、Python 数据分析、深度学习、向量空间等领域，通过 DeepSeek 大模型 API 提供真实的自然语言问答服务。

---

## 二、技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | React | 19 | 函数组件 + Hooks |
| 编程语言 | TypeScript | 5.8 | 全项目强类型 |
| 构建工具 | Vite | 6 | 开发服务器 + 生产构建 |
| CSS 框架 | Tailwind CSS | v4 | `@theme` 配置，无 tailwind.config.js |
| 动画库 | motion | 12 | 从 `motion/react` 导入 |
| 图标库 | lucide-react | 0.546 | 轻量级 SVG 图标 |
| Markdown 渲染 | react-markdown | 10 | AI 回复内容渲染 |
| 数学公式 | remark-math + rehype-katex + KaTeX | 6 / 7 / 0.17 | LaTeX 公式渲染 |
| AI 后端 | DeepSeek Chat API | — | 通过 Vite 代理调用 |
| 数据持久化 | localStorage | — | 对话历史、错题本、状态 |

---

## 三、系统架构

### 3.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                  浏览器 (SPA)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │  App.tsx  │  │  Pages   │  │  Components  │  │
│  │ (路由控制)│→│ (7个页面) │→│  (共享组件)   │  │
│  └──────────┘  └──────────┘  └──────────────┘  │
│         │              │                          │
│  ┌──────────┐  ┌──────────────┐                  │
│  │ Services │  │  localStorage │                  │
│  │(API+存储)│→│  (数据持久化)  │                  │
│  └──────────┘  └──────────────┘                  │
│         │                                        │
└─────────┼────────────────────────────────────────┘
          │ Vite Dev Server Proxy (/api/deepseek)
          ▼
┌─────────────────────┐
│  DeepSeek API       │
│  api.deepseek.com   │
└─────────────────────┘
```

### 3.2 核心设计决策

1. **无路由库**：页面切换由 `App.tsx` 中 `useState<Page>` 驱动，配合 `AnimatePresence` 实现过渡动画，避免引入 react-router 的复杂度
2. **4 Agent 对话隔离**：每个 AI Agent 拥有独立的对话列表和活跃对话 ID，按 `Record<AgentKey, Conversation[]>` 分区存储，切换 Agent 时自动切换上下文
3. **Vite 代理解决 CORS**：开发环境通过 `vite.config.ts` 的 `proxy` 配置将 `/api/deepseek` 请求代理到 `https://api.deepseek.com`，避免浏览器跨域限制
4. **localStorage 全量持久化**：对话历史、活跃对话 ID、错题本数据、保存/忽略状态均通过 localStorage 持久化，页面切换和刷新后数据不丢失
5. **Material Design 3 色彩体系**：通过 Tailwind CSS v4 的 `@theme` 块定义 primary/secondary/surface 等设计令牌，统一视觉风格

---

## 四、目录结构

```
tutor/
├── index.html                  # SPA 入口 HTML
├── vite.config.ts              # Vite 构建配置（代理、别名、环境变量）
├── tsconfig.json               # TypeScript 配置
├── package.json                # 依赖与脚本
├── .env.example                # 环境变量模板
├── AGENTS.md                   # AI Agent 开发指南
├── public/
│   └── videos/                 # 登录页视频背景资源
└── src/
    ├── main.tsx                # 应用入口（createRoot + StrictMode）
    ├── App.tsx                 # 主应用组件（侧边栏 + 导航 + 页面路由）
    ├── index.css               # 全局样式（Tailwind + KaTeX + 设计令牌）
    ├── pages/
    │   ├── Login.tsx           # 登录页（视频背景 + 表单）
    │   ├── Home.tsx            # AI 对话页（核心功能页）
    │   ├── WrongQuestions.tsx  # 错题本页
    │   ├── Discussion.tsx      # 讨论区页
    │   ├── HelpCenter.tsx      # 帮助中心页
    │   ├── Settings.tsx        # 设置页
    │   └── Profile.tsx         # 个人中心页
    ├── components/
    │   ├── VideoBackground.tsx # 视频背景组件（自适应分辨率）
    │   └── MessageContent.tsx  # 消息渲染组件（Markdown + LaTeX）
    ├── services/
    │   ├── deepseek.ts         # DeepSeek API 服务（4 Agent system prompt）
    │   └── wrongQuestions.ts   # 错题本 CRUD 服务
    └── config/
        └── videoConfig.ts      # 视频背景配置
```

---

## 五、核心功能模块

### 5.1 AI 多导师对话系统

- **四个专业 Agent**：高数酱（微积分/线代）、Py酱（Python/数据分析）、Torch君（深度学习）、矩阵妹（线性代数/向量空间）
- **独立对话隔离**：每个 Agent 维护独立的对话列表和活跃对话，互不干扰
- **DeepSeek API 集成**：每个 Agent 配备专属 system prompt，确保回答风格和专业领域匹配
- **多轮对话上下文**：发送消息时携带完整历史消息列表，API 可理解上下文
- **LaTeX 公式渲染**：AI 回复中的数学公式（`\frac{}{}`、`\partial` 等）通过 KaTeX 渲染为可视化公式，预处理 `\[...\]` → `$$...$$` 和 `\(...\)` → `$...$` 语法转换
- **对话历史管理**：右侧面板展示当前 Agent 的历史对话，支持点击切换、删除、新建对话
- **北京时间显示**：手动计算 UTC+8 时间，不依赖系统时区

### 5.2 错题本系统

- **一键保存**：AI 回复后显示"保存到错题本"和"不保存"两个操作按钮
- **智能提取**：保存时仅提取用户提问和 AI 回答，不存储完整对话上下文
- **状态持久化**：保存/忽略状态通过 localStorage 持久化，切换页面后状态不丢失
- **学科筛选**：错题本页面支持按学科（高数/Python/深度学习/线代）筛选
- **详情展开**：默认显示 AI 解答摘要，点击"查看详情"展开完整 Markdown 渲染内容
- **学科统计**：右侧面板展示各学科错题数量和占比

### 5.3 其他页面

- **登录页**：视频背景 + 品牌展示 + 登录表单（学生/教师切换、第三方登录）
- **讨论区**：帖子列表（最新/精华/未解决）、精华解析、活跃贡献者
- **帮助中心**：使用流程、FAQ、联系客服
- **设置页**：个人资料、学习设置、通用设置、账户安全
- **个人中心**：学习统计、最近动态、AI 导师卡片、成就勋章、目标进度

---

## 六、关键技术实现

### 6.1 DeepSeek API 调用

```typescript
// src/services/deepseek.ts
const DEEPSEEK_API_URL = '/api/deepseek/v1/chat/completions';  // 通过 Vite 代理

export async function chatWithDeepSeek(messages: ChatMessage[], agent: string): Promise<string> {
  const systemPrompt = agentSystemPrompts[agent];
  const response = await fetch(DEEPSEEK_API_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${API_KEY}` },
    body: JSON.stringify({ model: 'deepseek-chat', messages: [{ role: 'system', content: systemPrompt }, ...messages] }),
  });
  return (await response.json()).choices[0].message.content;
}
```

### 6.2 Agent 对话隔离

```typescript
// 按 AgentKey 分区存储
const [allConversations, setAllConversations] = useState<Record<AgentKey, Conversation[]>>({ ... });
const [allActiveIds, setAllActiveIds] = useState<Record<AgentKey, string | null>>({ ... });

// 切换 Agent 时自动读取对应分区
const conversations = allConversations[activeAgent];
const activeConversationId = allActiveIds[activeAgent];
```

### 6.3 LaTeX 公式预处理

```typescript
// src/components/MessageContent.tsx
function preprocessLatex(text: string): string {
  // DeepSeek 输出 \[...\] 和 \(...\)，remark-math 需要 $$...$$ 和 $...$
  text = text.replace(/\\\[([\s\S]*?)\\\]/g, (_, math) => `$$${math}$$`);
  text = text.replace(/\\\(([\s\S]*?)\\\)/g, (_, math) => `$${math}$`);
  return text;
}
```

### 6.4 状态竞态修复

```typescript
// 使用 useRef 追踪最新状态，避免 React 批量更新导致读取旧值
const allConversationsRef = useRef(allConversations);
allConversationsRef.current = allConversations;

// 发送 API 请求前通过 ref 读取最新消息列表
const currentConvs = allConversationsRef.current[activeAgent];
```

---

## 七、任务分工（5人）

### 成员一：前端架构与核心框架

**职责**：项目初始化、整体架构设计、核心框架搭建

| 任务项 | 具体内容 |
|--------|----------|
| 项目初始化 | 使用 Vite 6 + React 19 + TypeScript 搭建项目脚手架，配置 tsconfig.json（路径别名 `@/*`、experimentalDecorators） |
| 构建配置 | 编写 vite.config.ts（React 插件、Tailwind CSS v4 插件、环境变量注入、`@` 路径别名、API 代理） |
| 全局样式 | 编写 index.css（Tailwind 导入、KaTeX CSS 导入、`@theme` 设计令牌定义、毛玻璃/渐变工具类） |
| 应用入口 | 编写 App.tsx（侧边栏导航、顶部导航栏、`useState<Page>` 页面路由、AnimatePresence 过渡动画、登录状态管理） |
| 开发规范 | 编写 AGENTS.md 开发指南，定义项目约定和常用命令 |

### 成员二：AI 对话系统

**职责**：AI 多导师对话功能的设计与实现

| 任务项 | 具体内容 |
|--------|----------|
| API 服务层 | 编写 deepseek.ts（DeepSeek API 封装、4 个 Agent 专属 system prompt、错误处理） |
| 对话页面 | 编写 Home.tsx 核心对话界面（消息发送/接收、气泡式对话展示、加载状态） |
| Agent 隔离 | 实现按 AgentKey 分区的对话存储（`Record<AgentKey, Conversation[]>`），确保四个 Agent 对话独立 |
| 状态管理 | 修复 React 状态竞态问题（useRef 追踪最新状态、微任务等待），实现多轮对话上下文传递 |
| 对话历史 | 实现右侧对话历史面板（创建/切换/删除对话），localStorage 持久化，北京时间格式化 |

### 成员三：内容渲染与交互优化

**职责**：AI 回复内容渲染、公式显示、用户交互优化

| 任务项 | 具体内容 |
|--------|----------|
| Markdown 渲染 | 编写 MessageContent.tsx（react-markdown + remark-math + rehype-katex 集成） |
| LaTeX 公式 | 实现 LaTeX 分隔符预处理（`\[...\]` → `$$...$$`，`\(...\)` → `$...$`），确保 DeepSeek 输出的公式正确渲染 |
| 操作按钮 | 在 AI 回复后添加"保存到错题本"和"不保存"操作按钮，保存/忽略状态持久化到 localStorage |
| 样式优化 | 为 Markdown 渲染内容添加 Tailwind 样式（标题、列表、代码块、引用等），确保与整体设计一致 |
| 交互细节 | 输入框 Enter 发送、发送按钮禁用态、加载动画、滚动自动到底部 |

### 成员四：错题本与数据管理

**职责**：错题本功能的设计、实现与数据管理

| 任务项 | 具体内容 |
|--------|----------|
| 存储服务 | 编写 wrongQuestions.ts（基于 localStorage 的 CRUD 操作：读取/新增/删除/清空） |
| 错题本页面 | 重写 WrongQuestions.tsx（真实数据展示、时间线布局、学科筛选、详情展开/收起、删除功能） |
| 数据提取 | 实现保存到错题本时的智能提取逻辑（仅提取用户提问 + AI 回答，不存储完整对话） |
| 学科统计 | 实现右侧统计面板（各学科错题数量和占比动态计算） |
| 数据一致性 | 确保错题本数据与对话页面保存操作的一致性，处理 localStorage 读写异常 |

### 成员五：辅助页面与视觉设计

**职责**：非核心功能页面的实现与整体视觉设计

| 任务项 | 具体内容 |
|--------|----------|
| 登录页 | 编写 Login.tsx（视频背景 + 品牌展示 + 登录表单 + 第三方登录 + motion 动画） |
| 视频组件 | 编写 VideoBackground.tsx（自适应分辨率选择、加载状态处理、错误降级） |
| 讨论区 | 编写 Discussion.tsx（帖子列表、标签页切换、精华解析、活跃贡献者、发帖 FAB） |
| 帮助中心 | 编写 HelpCenter.tsx（搜索框、使用流程、FAQ 列表、联系客服） |
| 设置与个人中心 | 编写 Settings.tsx（四宫格设置布局）和 Profile.tsx（学习统计、动态时间线、成就勋章、目标进度） |

---

## 八、开发与部署

### 8.1 本地开发

```bash
npm install                    # 安装依赖
npm run dev                    # 启动开发服务器 (port 3000)
```

### 8.2 环境变量

在 `.env.local` 中配置：

```
GEMINI_API_KEY=your_key       # Gemini API Key（预留）
```

DeepSeek API Key 在 `src/services/deepseek.ts` 中硬编码。

### 8.3 构建与检查

```bash
npm run build                  # 生产构建
npm run lint                   # TypeScript 类型检查 (tsc --noEmit)
```

### 8.4 注意事项

- 修改 `vite.config.ts` 代理配置后需重启 dev server
- 项目无测试框架，无 ESLint，lint 命令仅执行类型检查
- `@` 路径别名指向项目根目录，导入如 `import x from '@/src/pages/Home'`
- Tailwind CSS v4 配置在 `src/index.css` 的 `@theme` 块中，不是 `tailwind.config.js`
